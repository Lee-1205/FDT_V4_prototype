[project]
name = "fdt-rlm"
version = "0.1.0"
description = "Causal Fuzzy Diffusion Transformer / Recursive Language Model research prototype"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

